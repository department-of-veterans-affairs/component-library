# Workflow name
name: 'Chromatic'

# Event for the workflow
on:
  push:
  pull_request:
    types: [opened, synchronize, reopened]

# List of jobs
jobs:
  chromatic-deployment:
    # Operating System
    runs-on: ubuntu-latest
    # Job steps
    steps:
      - uses: actions/checkout@v2
        with:
          fetch-depth: 0
      - name: Use Node.js 20.x
        uses: actions/setup-node@v2
        with:
          node-version: 20.x
          cache: 'yarn'
      - run: yarn install
      - name: Install dependencies in react-components
        run: yarn install
        working-directory: packages/react-components
      - run: yarn workspace @department-of-veterans-affairs/react-components run build
      - run: yarn workspace @department-of-veterans-affairs/web-components run build
      - run: yarn workspace @department-of-veterans-affairs/component-library run build
      - name: Publish to Chromatic
        id: chromatic
        uses: chromaui/action@latest
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          # 👇 Chromatic projectToken, refer to the manage page to obtain it.
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          onlyChanged: true
          workingDir: packages/storybook
          skip: 'dependabot/**'
          # debug: true
          # diagnosticsFile: chromatic-diagnostics.json
          # storybookLogFile: storybook-build.log
          branchName: ${{ env.FORMATTED_BRANCH_NAME }}
      # - name: Upload Storybook Build Log
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: storybook-build-log
      #     path: packages/storybook/storybook-build.log

      # - name: Download Storybook Build Log
      #   uses: actions/download-artifact@v4
      #   with:
      #     name: storybook-build-log
      #     path: packages/storybook

      # - name: Read Build Log File
      #   id: read-log
      #   run: |
      #     echo "==================== STORYBOOK BUILD LOG ==================="
      #     if [ -f "packages/storybook/storybook-build.log" ]; then
      #       cat packages/storybook/storybook-build.log
      #     else
      #       echo "Error: storybook-build.log file not found!"
      #       ls -la packages/storybook/
      #     fi
      #     echo "=========================================================="

      # - name: Use Extracted Branch Name
      #   run: echo "The branch name is ${{ env.branch_name }}"

      # - name: Publish Chromatic Summary
      #   run: echo -e "| Results |  |\n| --- | --- |\n| URL | ${{steps.chromatic.outputs.url}} |\n| Build Url | ${{steps.chromatic.outputs.buildUrl}} |\n| Storybook Url | ${{steps.chromatic.outputs.storybookUrl}} |\n| Change Count | ${{steps.chromatic.outputs.changeCount}} |" >> $GITHUB_STEP_SUMMARY
