{
  "name": "@raytest/test1",
  "version": "1.0.7",
  "description": "a test package",
  "packageManager": "yarn@3.2.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "rm -rf ./dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "demo": "echo DEMOING $npm_package_name v $npm_package_version with WORKSPACES",
    "demo2": "yarn build && npm publish publish --@raytest:registry=http://localhost:4873/"
  },
  "files": [
    "dist"
  ],
  "keywords": [],
  "author": "Ray M",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.8.7",
    "typescript": "^4.3.4"
  },
  "type": "module"
}
