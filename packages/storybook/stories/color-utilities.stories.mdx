import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks';
import { baseColors } from '../constants/colors';

<Meta title="Components/Color Utilities" />

This is a demo of the new utility classes

<Canvas>
  <Story name="Default">
  {baseColors.map(color => (
    <div key={color} className={`vads-u-padding--4 vads-u-margin-y--1 vads-bg-${color}`}>
      <code className="vads-bg-white">{`vads-bg-${color}`}</code>
    </div>
  ))}
  </Story>
</Canvas>
