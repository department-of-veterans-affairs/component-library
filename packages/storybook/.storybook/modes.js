export const allModes = {
    default: {
      // integer is just width
      viewport: 1008,
    },
    small: {
      // object can specify both
      viewport: {
        height: 896,
        width: 481,
      },
    },
    large: {
      // object with width
      viewport: {
        width: 1601,
      },
    }
  };